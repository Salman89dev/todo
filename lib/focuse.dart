import 'package:flutter/material.dart';

class FocusePage extends StatelessWidget {
  const FocusePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("FocusePage"),
    );
  }
}